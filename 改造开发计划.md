# OpenCodeX Proxy 改造计划 - 独立 Tauri 版本

## 1. 项目目标

开发一个**独立、干净、美观**的桌面应用，不再依赖并删除原 OpenCodeX 服务和 CLI。  
核心功能：让用户自由添加任意 LLM Provider，并在 Codex App 中使用这些模型。

---

## 2. 技术栈

- **桌面框架**：Tauri 2.x（推荐）
- **前端**：Svelte（推荐，轻量）或 Vue 3 + TypeScript
- **后端核心**：Python（复用原 proxy 逻辑）
- **通信**：Tauri Commands 或 HTTP
- **配置管理**：TOML / JSON
- **打包**：Tauri 官方打包

---

## 3. 核心功能（重点保留）

### 一键开启（Start）
- 启动 Proxy 服务
- 自动修改 Codex 配置（注入 Provider 和模型映射）
- 重启 Codex App

### 一键关闭（Stop）
- 停止 Proxy 服务
- 恢复原来的 Codex 配置
- 重启 Codex App（可选）

这两个功能作为**最核心、最醒目的操作**，在界面中以大按钮形式呈现。

---

## 4. 主要窗口（Pages）

1. **仪表盘 (Dashboard)**  
   - 当前状态显示  
   - **一键开启（Start）** 和 **一键关闭（Stop）** 大按钮  
   - 快速统计信息

2. **供应商管理 (Providers)**  
   - 添加/编辑/删除/测试 Provider

3. **模型管理 (Models)**  
   - 模型列表、启用禁用、参数编辑

4. **映射管理 (Mappings)**  
   - Codex 模型名称映射管理

5. **Codex 设置**  
   - 配置备份、恢复、手动干预

6. **日志 (Logs)**  
   - 实时日志查看

7. **设置 (Settings)**

---

## 5. 如何复用原 Proxy 代码

- 复制 `src/opencodex_proxy` 核心目录
- 重点保留：
  - Responses → Chat Completions 协议转换
  - Provider 管理和模型映射逻辑
  - Codex 配置读写与注入功能
- 将原 CLI 的 `start` / `stop` 逻辑封装成可调用的函数（`start_codex_mode()`、`stop_codex_mode()`）
- 删除所有 OpenCodeX 服务相关硬编码

---

## 6. 注意事项

- 必须保证 Codex 配置的**自动备份与安全恢复**
- Proxy 绑定 `127.0.0.1:8787`
- 优先保证一键 Start/Stop 的稳定性和用户体验
- 最终打包成单文件桌面应用（Windows .exe / macOS .app）

---

OpenCodeX Proxy项目地址：https://github.com/booboom/opencodex
